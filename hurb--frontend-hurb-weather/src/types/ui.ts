/**
 * Tipos para UI e preferências do usuário
 */

// Unidade de temperatura
export type TemperatureUnit = 'C' | 'F';

// Tema de gradiente de fundo baseado na temperatura
export type GradientTheme = 'cold' | 'warm' | 'hot' | 'neutral';
